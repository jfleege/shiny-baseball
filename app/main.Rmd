
```{r}
# stack
library(shiny) # to run app
library(tidyverse) # for different functions
library(bslib) # UI stuff
library(plotly) # interactive plots
library(Lahman) # data set
library(ggridges) # for team vs team comparison


# call files
source("helpers.R")
source("ui.R")
source("server.R")


# run app
shinyApp(ui = ui, server=server)
```
